@model IEnumerable<CleanArch.Application.ViewModels.CourseViewModel>

<div class="container">
    <div class="row">
        @foreach (var course in Model)
        {
            <span>@course.Name</span>
            <span>@course.Description</span>
        }
    </div>
</div>